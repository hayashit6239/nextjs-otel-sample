import { Flex, Text } from "@mantine/core";
import { FC } from "react"

const AuthorLayout: FC = () => {
    return (
        <Flex className="w-72 h-full">
            <Text>著者一覧</Text>
        </Flex>
    );
};

export default AuthorLayout;